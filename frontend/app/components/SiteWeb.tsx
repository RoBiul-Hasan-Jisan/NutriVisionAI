/** Global page background — intentionally minimal. The page surface itself
 *  (--page) carries the design; this component is kept only so the import
 *  in the root layout doesn't need to change. */
export function SiteWeb() {
  return null;
}
